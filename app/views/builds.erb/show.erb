<%= erb nav %>
<section class="intro-section">
    <div class= "container">
    <h1><%= @build.budget %></h1>
     <h1>Budget: <%= @build.budget %></h1>
     <ul>
        <% @project.parts.each do |part| %>
        <li><%= "#{part.name} - $#{part.price}" %>
            <span>

    <form action="/builds/<%= @build.id %>/parts/<%part.id %>/delete" method="post">
        <input type="hidden" id="hidden" name="_method" value="delete">
        <input type="submit" class="btn btn-danger" value="Delete Part">
            </form>
        </span>
      </li>
      <% end %>
    </ul>
    <button class="btn btn-default"><a href="/builds/<%= @build.id %>/parts/new"> Add New Part </a></button>
    <button class="btn btn-default"><a href="/builds/<%= @build.id %>/edit">Edit Build</a></button>

    <form action="/builds/<%= @build.id %>/delete" method="post">
        <input type="hidden" id="hidden" name="_method" value="delete">
        <input type="submit" class="btn btn-danger" value="delete project">
    </form>
    </div>
    </section>